package com.example.auraplay.ui.theme

import androidx.compose.ui.graphics.Color

// 🌤 LIGHT (Day) THEME — Minimal, Clean, Modern
val LightPrimary        = Color(0xFF6366F1) // Indigo — modern, vibrant
val LightSecondary      = Color(0xFF8B5CF6) // Purple — smooth accent
val LightTertiary       = Color(0xFFA5B4FC) // Light Indigo — soft highlight
val LightBackground     = Color(0xFFFAFAFA) // Off-white — clean base
val LightSurface        = Color(0xFFFFFFFF) // Pure White — minimal
val LightSurfaceVariant = Color(0xFFF5F5F5) // Light Gray — subtle neutral
val LightOnSurface      = Color(0xFF1F2937) // Dark Gray — readable text
val LightOnPrimary      = Color(0xFFFFFFFF) // White — clean contrast
val LightOnSecondary    = Color(0xFFFFFFFF) // White — consistent
val LightOutline        = Color(0xFFE5E7EB) // Light Gray — subtle dividers
val LightOutlineVariant = Color(0xFFD1D5DB) // Medium Gray — gentle edges
val LightShadow         = Color(0x1A000000) // Very soft shadow

// Accent and Utility Colors
val LightAccentEmerald  = Color(0xFF10B981) // Emerald — success/active
val LightAccentRuby     = Color(0xFFEF4444) // Red — errors/important
val LightAccentAmethyst = Color(0xFF8B5CF6) // Purple — highlights

// 🌙 DARK (Night) THEME — Smooth, Elegant, Modern
val DarkPrimary         = Color(0xFF818CF8) // Light Indigo — bright, modern
val DarkSecondary       = Color(0xFFA78BFA) // Light Purple — smooth accent
val DarkTertiary        = Color(0xFFC7D2FE) // Very Light Indigo — subtle highlight
val DarkBackground      = Color(0xFF0F172A) // Deep Slate — rich backdrop
val DarkSurface         = Color(0xFF1E293B) // Slate Gray — card surface
val DarkSurfaceVariant  = Color(0xFF334155) // Medium Slate — elevated surface
val DarkOnSurface       = Color(0xFFF1F5F9) // Light Gray — clean text
val DarkOnPrimary       = Color(0xFF0F172A) // Dark Slate — contrast
val DarkOnSecondary     = Color(0xFF0F172A) // Dark Slate — consistent
val DarkOutline         = Color(0xFF334155) // Medium Slate — subtle outline
val DarkOutlineVariant  = Color(0xFF475569) // Light Slate — visible edges
val DarkShadow          = Color(0x66000000) // Soft shadow

// Accent and Utility Colors
val DarkAccentEmerald   = Color(0xFF34D399) // Bright Emerald — success/active
val DarkAccentRuby      = Color(0xFFF87171) // Light Red — errors/important
val DarkAccentAmethyst  = Color(0xFFA78BFA) // Purple — highlights

// 🕊 Legacy (Optional Material reference)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
